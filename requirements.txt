yfinance>=0.2.50
lxml>=4.9.0
pandas>=2.0.0
numpy>=1.24.0
rich>=13.0.0
# Pinned: a saved GradientBoosting model (models/*.pkl) fails to unpickle across
# sklearn minor versions — 1.9.0 raised "ModuleNotFoundError: No module named
# '_loss'" against a 1.8.0 pickle, crash-looping the monitor on startup. Retrain
# the model and bump this together to upgrade.
scikit-learn==1.8.0
openai>=1.0.0
ib_insync>=0.9.86
tzdata>=2024.1
